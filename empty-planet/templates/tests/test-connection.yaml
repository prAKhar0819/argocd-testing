apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "empty-planet.fullname" . }}-test-connection"
  labels:
    {{- include "empty-planet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "empty-planet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
